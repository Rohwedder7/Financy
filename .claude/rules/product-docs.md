# Product documentation rule

Before changing behavior, read the SPEC and every BND/PRD/BR ID it references. Do not infer missing product decisions. Record a named `OPEN-*` question and stop that slice when the answer changes observable behavior.

Every behavior change must update its test and, when necessary, `docs/product/TRACEABILITY.md`.
