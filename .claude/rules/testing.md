# Testing rule

Write the failing test for each acceptance criterion before or with its implementation. Every protected resource requires a cross-user test. Do not replace integration tests with mocks of the behavior under test. Run the SPEC commands and report exact failures; never mark a gate as passing without execution.
