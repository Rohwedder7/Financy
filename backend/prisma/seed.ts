async function main() {
  // Seed records are introduced by the authentication and category SPECs.
  // Keeping this command deterministic and empty prevents fake credentials from entering the repository.
}

await main();
