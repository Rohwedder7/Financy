export function BrandMark({ className = '' }: { className?: string }) {
  return (
    <p className={`text-sm font-bold tracking-[0.18em] text-financy-green uppercase ${className}`}>
      Financy
    </p>
  )
}
